const EmptyView = () => {
  return (
    <div className="empty-view">
      Unfortunately, there were no results for your search. Try using a
      different search term.
    </div>
  );
};

export default EmptyView;
